package com.tunisie.pfe.repository;

import com.tunisie.pfe.entity.TypeEntreprise;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TypeEntrepriseRepository
        extends JpaRepository<TypeEntreprise, Long> {
}
